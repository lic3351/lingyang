const opentype = require('../dist/opentype');
const font = opentype.loadSync('./fonts/ZillaSlab-Regular.otf');

const glyphs = font.stringToGlyphs('Moz://a', {features: {liga: true, rlig: true, ornm: true}, language: 'latn'});
//console.log(font.tables.gsub);

const [glyph_a, glyph_b] = font.stringToGlyphs('//');
//const kv = font.getKerningValue(glyph_a, glyph_b);
const kv = font.getGposKerningValue(glyph_a, glyph_b);
console.log(kv);
